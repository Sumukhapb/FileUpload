<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Dashboard</title>
  </head>
  <body>
    <% layout('layout') %>

    <div class="container text-center mt-5">
      <div class="card shadow-lg p-4 mx-auto" style="max-width: 400px">
        <h2 class="mb-3">Welcome, <%= user.name %>!</h2>
        <img
          src="<%= user.profilPic %>"
          alt="Profile Picture"
          class="rounded-circle border shadow-sm mb-3"
          width="80"
          height="80"
        />
        <p class="text-muted">Email: <%= user.email %></p>
      </div>

      <div class="mt-4">
        <h4>Open Shared File</h4>
        <form action="/files/open-shared" method="GET" class="d-flex justify-content-center">
          <input
            type="text"
            name="link"
            class="form-control w-50 me-2"
            placeholder="Enter shareable link"
            required
          />
          <button type="submit" class="btn btn-primary">Open</button>
        </form>
      </div>
    </div>
  </body>
</html>
